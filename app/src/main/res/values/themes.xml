<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base -->
    <style name="Base.Theme.PawCare2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Puedes definir estilos del tema base aquí si los usas -->
    </style>

    <!-- Tema principal para modo claro -->
    <style name="Theme.PawCare2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores personalizados -->
        <item name="colorPrimary">@color/primary_light</item>
        <item name="colorPrimaryVariant">@color/primary_variant_light</item>
        <item name="colorOnPrimary">@color/on_primary_light</item>
        <item name="android:windowBackground">@color/background_light</item>
    </style>

</resources>
